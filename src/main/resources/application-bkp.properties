# Nombre de la aplicación
spring.application.name=levelupgamer

# Configuración de Oracle con Wallet
spring.datasource.url=jdbc:oracle:thin:@gd2366536a846ff_proyectogamer_high
spring.datasource.username=ADMIN
spring.datasource.password=Sth_s0p0rte##
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Configuración del Wallet - RUTA ABSOLUTA
spring.datasource.hikari.data-source-properties.oracle.net.wallet_location=/home/ubuntu/oracle_wallet
spring.datasource.hikari.data-source-properties.oracle.net.ssl_server_dn_match=true

# Configuración JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Configuración HikariCP
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000

# Servidor
server.port=8080
server.servlet.context-path=/

# Logging (opcional)
logging.level.com.example.levelupgamer=DEBUG